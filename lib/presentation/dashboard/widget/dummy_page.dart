import 'package:flutter/material.dart';
import 'package:payuung_pribadi_app/core/config/theme/app_colors.dart';

class SearchView extends StatelessWidget {
  const SearchView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.veryLightGrey,
      body: Center(
        child: Text('Search View'),
      ),
    );
  }
}

class CartView extends StatelessWidget {
  const CartView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.veryLightGrey,
      body: Center(
        child: Text('Cart View'),
      ),
    );
  }
}

class TrasactionView extends StatelessWidget {
  const TrasactionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.veryLightGrey,
      body: Center(
        child: Text('Trasaction View'),
      ),
    );
  }
}

class MyVoucherView extends StatelessWidget {
  const MyVoucherView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.veryLightGrey,
      body: Center(
        child: Text('MyVoucher View'),
      ),
    );
  }
}

class ShippingAddressView extends StatelessWidget {
  const ShippingAddressView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.veryLightGrey,
      body: Center(
        child: Text('ShippingAddress View'),
      ),
    );
  }
}

class FriendlistView extends StatelessWidget {
  const FriendlistView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.veryLightGrey,
      body: Center(
        child: Text('Friendlist View'),
      ),
    );
  }
}
